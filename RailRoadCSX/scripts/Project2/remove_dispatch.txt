# Run this while at the root of the view eg: /vob/dispatch etc..
for each in `ls -al | grep "kit" | awk '{print $9}'` ;
do
cd /vob/dispatch/$each
for dirs in `find . -type d | sort -r` ;
do
cd /vob/dispatch/$each/$dirs
cleartool rmelem -f *
done
cleartool rmelem -f /vob/dispatch/$each
done